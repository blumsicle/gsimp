# Example {{ .Name }} configuration.
#
# CLI flags override values loaded from this file.

# Zerolog log level for {{ .Name }} itself.
log_level: info

# Directory used by the application when a root path is needed.
root_path: .

# Optional git host and owner prefix used where a module path is needed.
git_location: ""
