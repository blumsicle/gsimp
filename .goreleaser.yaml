version: 2

project_name: bcli

builds:
  - id: bcli
    main: ./cmd/bcli
    binary: bcli
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
    goarch:
      - arm64

archives:
  - id: bcli
    ids:
      - bcli
    formats:
      - binary

checksum:
  name_template: checksums.txt
